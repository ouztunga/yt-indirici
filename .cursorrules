# Vibe Coding — Cursor Köprüsü (Legacy)

> **Tüm kurallar `AGENTS.md` dosyasındadır — önce onu oku.**
> Modüler kurallar: `.cursor/rules/` (100-vibe-core, 200-tech-stack, 300-planning, 400-security).
> NOT: Bu dosya yalnızca eski Cursor sürümleri için yedektir; güncel Cursor `.cursor/rules/` ve `AGENTS.md`'yi okur.

* **Dil:** TÜRKÇE — basit, jargonsuz.
* **İlk adım:** `/memory-bank/` dosyalarını ve `AGENTS.md`'yi oku.
* **Editör:** İçinde çalıştığın editörü tespit et; talimatları o editöre göre ver.
* **Akış:** Plan-before-Act — plan onaylanmadan kod yazma (`.cursor/rules/300-planning.mdc`).
